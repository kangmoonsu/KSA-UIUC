spring:
  application:
    name: ksa-backend
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true
  cloud:
    aws:
      s3:
        bucket: ksauiuc-test
      credentials:
        access-key: AKIAYJ7DQODZDFAAN2GL
        secret-key: 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
      region:
        static: "us-east-2" # Change if needed
  data:
    redis:
      host: ${SPRING_REDIS_HOST:localhost}
      port: ${SPRING_REDIS_PORT:6379}
  messages:
    basename: i18n/messages
    encoding: UTF-8

clerk:
  webhook:
    secret: whsec_7ewuaMWoawAVeC6BE3Wsed/h0sNra3Ov

server:
  tomcat:
    uri-encoding: UTF-8
